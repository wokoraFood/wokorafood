# Local SQLite (default). Hostinger VPS: keep this file DB on the box.
# docker compose -f docker-compose.vps.yml up -d --build
DATABASE_URL="file:./dev.db"
# Optional Postgres on the same VPS (docker-compose.yml):
# DATABASE_URL="postgresql://wokora:wokora@localhost:5432/wokorafoods?schema=public"
STORE_ID="wokora"

NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="replace-with-a-long-random-string"

GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

PRINT_MODE="kiosk"
PRINT_AGENT_URL="http://127.0.0.1:4100"
PRINT_AGENT_SECRET="replace-with-a-print-agent-secret"
PRINTER_IP=""
PRINTER_PORT="9100"

WHATSAPP_NOTIFY="false"
WHATSAPP_TOKEN=""
WHATSAPP_PHONE_ID=""
